name: Self-hosted Runners Example
  push:
    branches:
      - main

jobs:
  run_scripts:
    name: Execute Scripts
    runs-on: self-hosted

    steps:
      - name: Clone repository
        uses: actions/checkout@v2

      - name: Run Script 1
        run: ls
     
