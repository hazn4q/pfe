stages:     
  - build
  - test

build-job:       
  stage: build
  tags:
    - linux
  script:
    - echo "Compiling thkSessqsSDdsSDF cgholes..."
    - pwd
    - touch myfile.txt
    - ls
  artifacts:
    paths:
      - ./myfile.txt

unit-test-job: 
  tags:
    - linux
  stage: test    
  script:
    - echo "Running unit tests..."
    - ls
    - test -f myfile.txt

