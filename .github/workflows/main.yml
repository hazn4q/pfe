name: Run Script on Local Machinee
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Script
        run: |
          # Execute your script commands here
          python3 validation.py
