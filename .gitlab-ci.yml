stages:
  - deploy

deploy:
  stage: deploy
  tags: 
    - linux_runner
  before_script:

    - apt-get update -qq
    - apt-get install -y ansible
    - pip install ansible-pylibssh

  script:
    - ansible-playbook -i hosts.yaml déploiement.yaml


